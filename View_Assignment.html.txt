<!-- ==================== ASSIGNMENT VIEW ==================== -->
<div id="assignment-view" class="flex-1 flex flex-col hidden h-full overflow-hidden animate-fade-in relative z-10 w-full max-w-7xl mx-auto p-4 md:p-6 gap-6">
    <!-- Header -->
    <div class="bg-white p-4 md:p-6 rounded-2xl border border-gray-200 shadow-sm flex flex-col md:flex-row items-center justify-between gap-4">
        <div>
            <div class="flex items-center gap-3">
                <button onclick="goHome()" class="w-10 h-10 rounded-xl bg-gray-50 text-gray-500 hover:bg-gray-100 hover:text-gray-800 transition-colors flex items-center justify-center">
                    <i class="fa-solid fa-arrow-left"></i>
                </button>
                <h2 class="text-2xl font-bold text-gray-800 usmp-title">Asignación de Coordinadores</h2>
            </div>
            <p class="text-sm text-gray-500 mt-1 ml-13">Seleccione y guarde el coordinador responsable para cada asignatura.</p>
        </div>
        <div class="flex items-center gap-4 bg-gray-50 px-4 py-2 rounded-lg border border-gray-100">
             <div class="text-xs text-gray-500 font-bold uppercase tracking-wide">Usuario Activo:</div>
             <div class="flex items-center gap-2">
                 <div class="w-8 h-8 rounded-full bg-purple-100 text-purple-600 flex items-center justify-center font-bold text-xs" id="assign-user-avatar">
                     U
                 </div>
                 <div class="flex flex-col">
                     <span class="text-sm font-bold text-gray-800" id="assign-user-name">Cargando...</span>
                     <span class="text-[10px] text-gray-500" id="assign-user-role">Cargando...</span>
                 </div>
             </div>
        </div>
    </div>



    <!-- ACCIONES GLOBALES DE BASE DE DATOS -->
    <div id="assign-global-actions" class="flex flex-col md:flex-row gap-3 justify-end items-center">
        <button id="btn-sync-matriz" onclick="ejecutarImportacionBD()" class="w-full md:w-auto px-5 py-2.5 bg-blue-50 hover:bg-blue-100 text-blue-700 font-bold text-sm rounded-xl border border-blue-200 transition-colors flex items-center justify-center gap-2 shadow-sm">
            <i class="fa-solid fa-cloud-arrow-down"></i> Actualizar BD desde Matriz
        </button>
        <button id="btn-sync-lms" onclick="ejecutarSyncLMS()" class="w-full md:w-auto px-5 py-2.5 bg-green-50 hover:bg-green-100 text-green-700 font-bold text-sm rounded-xl border border-green-200 transition-colors flex items-center justify-center gap-2 shadow-sm">
            <i class="fa-solid fa-rotate"></i> Ejecutar / Actualizar Asignación
        </button>
    </div>

    <!-- Buscador / Filtros -->
    <div class="bg-white p-4 rounded-xl border border-gray-200 shadow-sm flex flex-col md:flex-row gap-4">
        <div class="relative flex-1">
            <i class="fa-solid fa-search absolute left-4 top-1/2 -translate-y-1/2 text-gray-400"></i>
            <input type="text" id="assign-search" oninput="renderAssignmentList()" placeholder="Buscar por asignatura, programa, docente..." class="w-full pl-11 pr-4 py-2.5 bg-gray-50 border border-gray-200 rounded-lg text-sm focus:outline-none focus:ring-2 focus:ring-purple-500 focus:bg-white transition-all">
        </div>
        <div class="flex items-center text-sm font-bold text-gray-400 bg-gray-50 px-4 py-2.5 rounded-lg border border-gray-100" id="assign-count">
            0 REGISTROS
        </div>
    </div>
    
    <!-- DASHBOARD DE RESUMEN (Fase 3.1) -->
    <div class="bg-white p-4 rounded-xl border border-gray-200 shadow-sm flex flex-col lg:flex-row gap-6">
        <!-- Contenedor del Gráfico -->
        <div class="lg:w-1/3 flex flex-col items-center justify-center border-b lg:border-b-0 lg:border-r border-gray-100 pb-4 lg:pb-0 lg:pr-6">
            <h3 class="text-sm font-bold text-gray-700 mb-2 w-full text-center">Distribución de Carga</h3>
            <div class="relative w-48 h-48">
                <canvas id="assignChart"></canvas>
            </div>
        </div>
        
        <!-- Contenedor de Tarjetas KPI -->
        <div class="flex-1">
             <h3 class="text-sm font-bold text-gray-700 mb-3">Resumen por Coordinador</h3>
             <div id="assign-kpi-container" class="grid grid-cols-1 sm:grid-cols-2 xl:grid-cols-3 gap-3 overflow-y-auto max-h-48 pr-2 custom-scrollbar">
                 <!-- Tarjetas generadas dinámicamente -->
             </div>
        </div>
    </div>

    <!-- ASIGNACIÓN MASIVA POR PROGRAMA (Fase 3.2) -->
    <div id="assign-bulk-bar" class="bg-purple-50 p-4 rounded-xl border border-purple-200 shadow-sm flex flex-wrap items-center gap-4">
        <div class="flex items-center gap-2 text-purple-700 font-bold shrink-0 w-full md:w-auto">
            <i class="fa-solid fa-layer-group text-xl"></i> Asignación Masiva:
        </div>
        <select id="bulk-program-select" class="flex-1 min-w-[200px] text-sm bg-white border border-purple-300 rounded-lg p-2.5 focus:ring-2 focus:ring-purple-500 outline-none">
            <option value="">-- Seleccionar Programa --</option>
            <!-- Llenado dinámicamente -->
        </select>
        <span class="text-purple-400 font-bold hidden xl:inline shrink-0"><i class="fa-solid fa-arrow-right"></i></span>
        <select id="bulk-coord-select" class="flex-1 min-w-[200px] text-sm bg-white border border-purple-300 rounded-lg p-2.5 focus:ring-2 focus:ring-purple-500 outline-none">
            <option value="">-- Seleccionar Responsable --</option>
            <!-- Llenado dinámicamente con coordinadores/jefes -->
        </select>
        <button id="btn-bulk-save" onclick="saveBulkCoord()" class="w-full xl:w-auto shrink-0 bg-purple-600 hover:bg-purple-700 text-white font-bold py-2.5 px-6 rounded-lg shadow-sm transition-colors text-sm flex items-center justify-center gap-2">
            <i class="fa-solid fa-bolt"></i> Asignar Todos
        </button>
    </div>

    <!-- Lista de Asignaturas (Cards) -->
    <div class="flex-1 overflow-y-auto pr-2 custom-scrollbar">
        <div id="assign-list" class="flex flex-col gap-3 pb-8">
             <!-- Se llena dinámicamente -->
        </div>
    </div>
</div>
